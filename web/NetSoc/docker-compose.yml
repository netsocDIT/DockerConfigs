version: '2'

services:
  netsoc_web:
    image: httpd
    restart: unless-stopped
    environment:
      VIRTUAL_HOST: netsoc.dit.ie
      LETSENCRYPT_HOST: netsoc.dit.ie
      LETSENCRYPT_EMAIL: netsoc@socs.dit.ie
    volumes:
     - /Docker/storage/NetSoc.dit.ie:/usr/local/apache2/htdocs/

networks:
  default:
    external:
      name: nginx-proxy
